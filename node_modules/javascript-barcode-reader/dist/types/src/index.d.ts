import { BARCODE_DECODERS } from './utilities/BARCODE_DECODERS';
import { ImageDataLike } from './utilities/ImageDataLike';
declare type JavascriptBarcodeReader = {
    image: string | HTMLImageElement | HTMLCanvasElement | ImageDataLike;
    barcode: string | BARCODE_DECODERS;
    barcodeType?: string;
    options?: {
        useAdaptiveThreshold?: boolean;
        singlePass?: boolean;
    };
};
export default function javascriptBarcodeReader({ image, barcode, barcodeType, options, }: JavascriptBarcodeReader): Promise<string>;
export {};
